apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-backend-deployment
  labels:
    app: auth-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-backend
  template:
    metadata:
      labels:
        app: auth-backend
    spec:
      imagePullSecrets:
        - name: awsecr-cred
      containers:
        - name: auth-backend
          image: 457436777387.dkr.ecr.eu-west-1.amazonaws.com/auth-backend:1c5e1505a75fcc7cfdaf96641690ace8e3f69f56
          ports:
            - containerPort: 3002
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: '3002'
            - name: MONGO_URI
              valueFrom:
                configMapKeyRef:
                  name: mongo-configmap
                  key: uri
            - name: AUTH_MONGO_USERNAME
              valueFrom:
                secretKeyRef:
                  name: auth-mongo-secret
                  key: username
            - name: AUTH_MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-mongo-secret
                  key: password
            - name: ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tokens-secret
                  key: access-token
            - name: REFRESH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tokens-secret
                  key: refresh-token
---
apiVersion: v1
kind: Service
metadata:
  name: auth-backend-service
spec:
  selector:
    app: auth-backend
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
