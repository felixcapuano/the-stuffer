apiVersion: apps/v1
kind: Deployment
metadata:
  name: thestuffer-backend-deployment
  labels:
    app: thestuffer-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thestuffer-backend
  template:
    metadata:
      labels:
        app: thestuffer-backend
    spec:
      imagePullSecrets:
        - name: awsecr-cred
      containers:
        - name: thestuffer-backend
          image: 457436777387.dkr.ecr.eu-west-1.amazonaws.com/thestuffer-backend:latest
          ports:
            - containerPort: 3001
          env:
            - name: STUFF_PORT
              value: '3001'
            - name: THESTUFFER_HOST
              value: thestuffer-frontend
            - name: THESTUFFER_PORT
              value: '3000'
            - name: MONGO_URI
              valueFrom:
                configMapKeyRef:
                  name: mongo-configmap
                  key: uri
            - name: STUFF_MONGO_USERNAME
              valueFrom:
                secretKeyRef:
                  name: stuff-mongo-secret
                  key: username
            - name: STUFF_MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: stuff-mongo-secret
                  key: password
            - name: ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tokens-secret
                  key: access-token
            - name: REFRESH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tokens-secret
                  key: refresh-token
---
apiVersion: v1
kind: Service
metadata:
  name: thestuffer-backend-service
spec:
  selector:
    app: thestuffer-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
